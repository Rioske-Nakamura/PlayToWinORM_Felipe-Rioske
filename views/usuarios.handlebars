<h1>usuario Cadastrados</h1>

<table border="1">
    <tr>
        <th>Id</th>
        <th>Username</th>
        <th>nome</th>
    </tr>
    {{#each usuarios}}
        <tr>
            <td>{{this.id}}</td>
            <td>{{this.nickname}}</td>
            <td>{{this.nome}}</td>
        </tr>
    {{/each}}
</table>